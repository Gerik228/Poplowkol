declare
  l_clob     clob;
  l_ctx      apex_exec.t_context;
  l_bind_val apex_exec.t_values;
begin
  -- Подготавливаем bind-параметры
  apex_exec.add_value(p_values => l_bind_val, p_value => apex_application.g_x01);

  -- Открываем запрос с параметрами
  l_ctx := apex_exec.open_query_context(
    p_location     => apex_exec.c_location_local_db,
    p_sql_query    => q'~
      select des_test
        from bi_portal_faq
       where id = to_number(:x01)
    ~',
    p_max_rows     => 1,
    p_bind_names   => apex_util.string_to_table('X01'),
    p_bind_values  => l_bind_val
  );

  if apex_exec.next_row(l_ctx) then
    l_clob := apex_exec.get_clob(l_ctx, 1);

    apex_json.open_object;
    apex_json.write('html', l_clob); -- вернётся в JS
    apex_json.close_object;
  else
    apex_json.open_object;
    apex_json.write('error', 'Контент не найден');
    apex_json.close_object;
  end if;

  apex_exec.close(l_ctx);

exception
  when others then
    apex_exec.close(l_ctx);
    apex_json.open_object;
    apex_json.write('error', sqlerrm);
    apex_json.close_object;
end;