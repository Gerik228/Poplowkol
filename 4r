DECLARE
  l_url VARCHAR2(4000);
BEGIN
  -- Получаем URL
  l_url := apex_util.get_blob_file_src('P31_IMAGE', :P31_ID);

  -- Обновляем таблицу
  UPDATE news_test
  SET url = l_url
  WHERE id = :P31_ID;
END;