<div class="glass-nav-container">
    <div class="glass-card">
        <div class="glass-backdrop"></div>
        <div class="glass-content">
            <div class="for_text">
                <h3 class="glass-title">Найти интересующий дашборд</h3>
                <p class="glass-description">Для вашего удобства дашборды собраны в едином пространстве</p>
            </div>
            <div class="gr_icon">
                <img class="img_ic" src="https://www.sberbank.ru/images/13300/1734425687306.png" alt="Dashboard Icon" />
            </div>
        </div>
    </div>
    
    <div class="glass-card">
        <div class="glass-backdrop"></div>
        <div class="glass-content">
            <div class="for_text">
                <h3 class="glass-title">Найти интересующий дашборд</h3>
                <p class="glass-description">Для вашего удобства дашборды собраны в едином пространстве</p>
            </div>
            <div class="gr_icon">
                <img class="img_ic" src="https://www.sberbank.ru/images/13300/1734425687306.png" alt="Dashboard Icon" />
            </div>
        </div>
    </div>
    
    <div class="glass-card">
        <div class="glass-backdrop"></div>
        <div class="glass-content">
            <div class="for_text">
                <h3 class="glass-title">Найти интересующий дашборд</h3>
                <p class="glass-description">Для вашего удобства дашборды собраны в едином пространстве с расширенным описанием и дополнительными возможностями для более удобного использования системы</p>
            </div>
            <div class="gr_icon">
                <img class="img_ic" src="https://www.sberbank.ru/images/13300/1734425687306.png" alt="Dashboard Icon" />
            </div>
        </div>
    </div>
</div>

<style>
/* Основной контейнер с сеткой */
.glass-nav-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 24px;
    padding: 20px;
    background: linear-gradient(135deg, #00f4c6 0%, #000f4c 20%, #00f4c 40%, #bd3300 100%);
    min-height: 100vh;
    box-sizing: border-box;
}

/* Карточка с glass эффектом */
.glass-card {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 20px;
    padding: 0;
    position: relative;
    overflow: hidden;
    cursor: pointer;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    width: 100%;
    min-height: 200px;
    height: auto;
    display: flex;
    flex-direction: column;
}

/* Эффект при наведении */
.glass-card:hover {
    transform: translateY(-8px);
    background: rgba(255, 255, 255, 0.25);
    border-color: rgba(255, 255, 255, 0.4);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
}

/* Декоративная полоса сверху */
.glass-backdrop {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.6), transparent);
    z-index: 1;
}

/* Основной контент карточки */
.glass-content {
    display: flex;
    flex: 1;
    align-items: stretch;
    min-height: 0;
    position: relative;
}

/* Текстовая часть */
.for_text {
    flex: 1;
    display: flex;
    flex-direction: column;
    padding: 24px;
    padding-right: 12px;
    justify-content: flex-start;
    min-width: 0;
    box-sizing: border-box;
}

/* Контейнер для иконки */
.gr_icon {
    flex-shrink: 0;
    width: 120px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 24px 24px 24px 12px;
    position: relative;
    overflow: hidden;
}

/* Изображение иконки */
.img_ic {
    width: 80px;
    height: 80px;
    object-fit: contain;
    border-radius: 12px;
    transition: transform 0.3s ease;
}

.glass-card:hover .img_ic {
    transform: scale(1.1);
}

/* Заголовок */
.glass-title {
    font-size: 20px;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.95);
    margin: 0 0 12px 0;
    line-height: 1.3;
    word-wrap: break-word;
    hyphens: auto;
}

/* Описание */
.glass-description {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.6;
    margin: 0;
    word-wrap: break-word;
    hyphens: auto;
    overflow-wrap: break-word;
    display: -webkit-box;
    -webkit-line-clamp: 4;
    -webkit-box-orient: vertical;
    overflow: hidden;
    text-overflow: ellipsis;
}

/* Адаптивные стили для планшетов */
@media (max-width: 768px) {
    .glass-nav-container {
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        padding: 16px;
    }
    
    .for_text {
        padding: 20px;
        padding-right: 10px;
    }
    
    .gr_icon {
        width: 100px;
        padding: 20px 20px 20px 10px;
    }
    
    .img_ic {
        width: 70px;
        height: 70px;
    }
    
    .glass-title {
        font-size: 18px;
    }
    
    .glass-description {
        font-size: 13px;
        -webkit-line-clamp: 3;
    }
}

/* Адаптивные стили для мобильных устройств */
@media (max-width: 480px) {
    .glass-nav-container {
        grid-template-columns: 1fr;
        gap: 16px;
        padding: 12px;
    }
    
    .glass-content {
        flex-direction: column;
        align-items: stretch;
    }
    
    .for_text {
        padding: 20px;
        padding-bottom: 12px;
        text-align: center;
    }
    
    .gr_icon {
        width: auto;
        padding: 12px 20px 20px 20px;
        justify-content: center;
    }
    
    .img_ic {
        width: 60px;
        height: 60px;
    }
    
    .glass-title {
        font-size: 16px;
        margin-bottom: 8px;
    }
    
    .glass-description {
        font-size: 12px;
        -webkit-line-clamp: 3;
    }
}

/* Стили для очень маленьких экранов */
@media (max-width: 320px) {
    .glass-nav-container {
        padding: 8px;
        gap: 12px;
    }
    
    .for_text {
        padding: 16px;
        padding-bottom: 8px;
    }
    
    .gr_icon {
        padding: 8px 16px 16px 16px;
    }
    
    .img_ic {
        width: 50px;
        height: 50px;
    }
}

/* Улучшения для больших экранов */
@media (min-width: 1200px) {
    .glass-nav-container {
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 30px;
        padding: 40px;
    }
    
    .glass-card {
        min-height: 220px;
    }
    
    .for_text {
        padding: 30px;
        padding-right: 15px;
    }
    
    .gr_icon {
        width: 140px;
        padding: 30px 30px 30px 15px;
    }
    
    .img_ic {
        width: 90px;
        height: 90px;
    }
    
    .glass-title {
        font-size: 22px;
        margin-bottom: 16px;
    }
    
    .glass-description {
        font-size: 15px;
        -webkit-line-clamp: 5;
    }
}

/* Стили для печати */
@media print {
    .glass-nav-container {
        background: white;
        grid-template-columns: repeat(2, 1fr);
    }
    
    .glass-card {
        background: white;
        border: 2px solid #ccc;
        backdrop-filter: none;
        -webkit-backdrop-filter: none;
    }
    
    .glass-title,
    .glass-description {
        color: black;
    }
}

/* Дополнительная поддержка старых браузеров */
@supports not (backdrop-filter: blur(20px)) {
    .glass-card {
        background: rgba(255, 255, 255, 0.25);
    }
}
</style>
